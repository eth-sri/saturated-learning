[project]
name = "saturated-data-training"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.5",
    "transformers>=4.46",
    "trl>=0.12",
    "peft>=0.13",
    "datasets>=3.0",
    "accelerate>=1.0",
    "vllm>=0.11",
    "wandb",
    "python-dotenv",
    "numpy",
    "bitsandbytes>=0.44",
]
